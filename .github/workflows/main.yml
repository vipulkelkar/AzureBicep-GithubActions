on:
  workflow_dispatch:
    inputs:
        environment:
          description: 'Provide the release environment'
          required: true
          type: environment
env:
  parameters.location.value: 'east us'
  parameters.storageAccountName.value: amstststg{{github.event.inputs.environment}}
  parameters.appInsightName.value: amststappinsight{{github.event.inputs.environment}}
  parameters.appServicePlanName.value: amstest{{github.event.inputs.environment}}-asp
  parameters.functionAppName.value: amststfuncapp{{github.event.inputs.environment}}
  parameters.keyVaultName.value: amststvault{{github.event.inputs.environment}}
  parameters.keyVaultSecretName.value: amstestsecret
  parameters.vnetName.value: amstestVnet{{github.event.inputs.environment}}
  parameters.subnetName.value: amstestSubnet{{github.event.inputs.environment}}
  parameters.keyVaultSecretValue.value: topsecretvalue
  parameters.tenantId.value: 3a6e195f-a33b-4031-a66a-0434183703d9
  parameters.environment.value: DEV
  parameters.resourceGroup.value: amsPune-{{github.event.inputs.environment}}
  resourceGroup: amsPune-{{github.event.inputs.environment}}

name: My App Deployment
jobs:
  create-azure-resources:
    runs-on: ubuntu-latest
    steps:

      # Checkout code
    - uses: actions/checkout@v3
      with:
        ref: dev

      # Log into Azure
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - uses: microsoft/variable-substitution@v1 
      with:
        files: './appDeployment/parameters.json'

      # Deploy Bicep file
    - name: deploy bicep file
      uses: azure/arm-deploy@v1
      with:
        subscriptionId: ${{ secrets.AZURE_SUBSCRIPTION }}
        resourceGroupName: ${{ env.resourceGroup }}
        template: ./appDeployment/main.bicep
        parameters: ./appDeployment/parameters.json
        failOnStdErr: false
